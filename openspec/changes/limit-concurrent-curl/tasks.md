- [x] Add `maxActiveHandles` field to `curlFileTransfer` with `httpConnections * 5` cap
- [x] Handle `http-connections = 0` by falling back to `hardware_concurrency()`
- [x] Add queue-size check in `workerThreadMain` incoming drain loop
- [x] Schedule 100ms wakeup when queue is full (not busy-wait)
- [x] Convert `Activity act` to `std::unique_ptr<Activity> _act` for lazy init
- [x] Add `act()` accessor that lazily creates the Activity and resets `startTime`
- [x] Add `resolverCallbackWrapper` to trigger `act()` on resolver start
- [x] Guard `CURLOPT_RESOLVER_START_FUNCTION` with `LIBCURL_VERSION_NUM >= 0x073b00`
- [x] Change `act.progress(...)` calls to `act().progress(...)` (2 sites)
- [x] Build passes
